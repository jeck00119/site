# Windows-specific requirements
# Include all base requirements
-r requirements.txt

# Windows-specific packages
pywin32>=306  # Windows Management Instrumentation (WMI) access

# Additional Windows packages
# DirectShow support is included with opencv-python
# No additional packages typically needed for basic camera support