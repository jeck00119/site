# Windows-specific Python requirements for CNC Project
# Use this file if you encounter issues with the main requirements.txt on Windows

# Core dependencies
aiofiles>=23.0.0
anyio>=3.6.0
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Authentication and security
PyJWT>=2.6.0
bcrypt>=4.0.0
cryptography>=40.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
sqlalchemy>=2.0.0

# Web and networking
starlette>=0.27.0
python-multipart>=0.0.6
websockets>=11.0.0
wsproto>=1.2.0

# File handling
python-dotenv>=1.0.0
openpyxl>=3.1.0

# Image processing (Windows-compatible versions)
Pillow>=9.5.0
opencv-python>=4.7.0
scikit-image>=0.20.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# Machine learning (optional - comment out if causing issues)
torch>=2.0.0; platform_machine != "arm64"
torchvision>=0.15.0; platform_machine != "arm64"

# Computer vision and OCR
easyocr>=1.7.0
pytesseract>=0.3.10

# Hardware interfaces
pyserial>=3.5
pygame>=2.5.0

# Utilities
humps>=0.2.2
shortuuid>=1.0.11
imutils>=0.5.4
filelock>=3.12.0

# Specialized libraries (may require additional setup)
# Uncomment if needed and ensure proper installation
# pylibdmtx>=0.1.10
# pyzbar>=0.1.9
# pymycobot>=3.0.0
# pypylon>=1.9.0
# xarm>=1.11.0

# Development and testing
transitions>=0.9.0

# Additional packages for specific features
# patched-yolo-infer  # Custom package - install manually if needed
# segment-anything-py  # Custom package - install manually if needed
# supervision  # Custom package - install manually if needed
# ttach  # Custom package - install manually if needed
# ultralytics  # YOLO package - install manually if needed

# PyQt5 for GUI (Windows installer recommended)
# PyQt5>=5.15.0  # Install from official installer if pip fails

